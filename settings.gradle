pluginManagement {
    repositories {
        jcenter()

        mavenCentral()
        google()
        maven{url("https://jitpack.io")}
        maven{url("https://maven.google.com")}
        maven { url 'https://android-sdk.is.com/' }
    }
}

include ':app', ':iosSwitch'
