package com.puddy.board.util.enums

import com.puddy.board.R

enum class LanguageChange(val displayName: Int) {
    SWIPE_THROUGH_SPACE(R.string.language_change_swipe_through_space),
    SPECIAL_BUTTON(R.string.language_change_special_button)
}
